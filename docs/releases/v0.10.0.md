# v0.10.0 发布说明

本次版本对应自动化持续优化流程的第 3 步：补充 GitHub Secrets 配置说明。

## 新增

- 新增 `docs/github-secrets.md`。
- 说明 GitHub Actions 默认 `GITHUB_TOKEN` 的用途与权限要求。
- 补充 `ANTHROPIC_API_KEY` 与 `OPENAI_API_KEY` 的可选配置说明。

## 优化

- README 增加 GitHub Secrets 入口说明。
- 版本号更新为 `v0.10.0`。

## 验证

- 已执行 `npm run build` 与 `npm run check`。
