# v0.12.0 发布说明

本次版本对应自动化持续优化流程的第 5 步：创建本地循环辅助脚本。

## 新增

- 新增 `optimize-loop.sh`。
- 脚本支持每小时循环执行提交、推送和 GitHub Release 创建。
- README 增加本地循环脚本使用说明。

## 优化

- 版本号更新为 `v0.12.0`。
- 为本地持续优化提供辅助入口。

## 验证

- 已执行 `npm run build` 与 `npm run check`。
