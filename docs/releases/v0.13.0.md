# v0.13.0 发布说明

本次版本对应自动化持续优化流程的第 6 步：测试完整流程。

## 新增

- 新增 `docs/flow-test.md`。
- 补充手动触发 GitHub Actions 的测试步骤。
- 补充使用 `AGENT_OPTIMIZE_PROMPT.md` 执行一轮优化的说明。
- 补充检查 push、README、RELEASE_NOTES 和 GitHub Release 的验证清单。
- 补充本地 `optimize-loop.sh` 测试方式。

## 优化

- README 增加流程测试入口说明。
- 版本号更新为 `v0.13.0`。

## 验证

- 已执行 `npm run build` 与 `npm run check`。
