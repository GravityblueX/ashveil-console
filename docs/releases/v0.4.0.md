# v0.4.0 发布说明

这次版本围绕高级筛选搜索进行优化，让每个模块页的数据查找更接近真实后台的使用方式。

## 新增

- 新增 `FilterBar` 高级筛选工具栏组件。
- 支持关键词搜索当前模块的所有字段。
- 支持根据状态/级别筛选用户、任务、审计日志和监控节点。
- 支持审计日志按渠道筛选。
- 表格上方新增总数与筛选结果统计。

## 优化

- 模块切换时自动重置筛选条件，避免跨模块残留。
- 筛选工具栏适配移动端窄屏布局。
- README 更新为 `v0.4.0` 的简体中文介绍。

## 验证

- 已执行 `npm run check`，前端构建通过。
