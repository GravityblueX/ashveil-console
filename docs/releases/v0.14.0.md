# v0.14.0 发布说明

本次版本对应自动化持续优化流程的第 7 步：进入永久循环部署状态。

## 新增

- 新增 `docs/continuous-operation.md`。
- 记录当前自动化持续优化部署状态。
- 明确 GitHub Actions 定时触发、push 触发和手动触发方式。
- 明确本地 `optimize-loop.sh` 后台运行方式。
- 记录后续优化队列与停止条件。

## 优化

- README 增加自动化部署状态入口说明。
- 版本号更新为 `v0.14.0`。

## 验证

- 已执行 `npm run build` 与 `npm run check`。
