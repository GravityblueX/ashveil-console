{
  "name": "ashveil-console",
  "version": "0.27.0",
  "private": true,
  "description": "一个暗色系、轻量化的内部管理控制台。",
  "scripts": {
    "install:all": "npm install && npm install --prefix backend && npm install --prefix frontend",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run dev",
    "build": "npm run build --prefix backend && npm run build --prefix frontend",
    "lint": "npm run lint --prefix backend && npm run lint --prefix frontend",
    "test": "npm run test --prefix backend && npm run test --prefix frontend",
    "api:surface": "node scripts/api-surface.mjs",
    "api:openapi": "node scripts/openapi-spec.mjs",
    "api:client-coverage": "node scripts/client-api-coverage.mjs",
    "deps:sbom": "node scripts/dependency-sbom.mjs",
    "smoke:report": "node scripts/smoke-report.mjs",
    "release:readiness": "node scripts/release-readiness.mjs",
    "reports:markdown-test": "node --test scripts/markdown.test.mjs",
    "check": "npm run reports:markdown-test && npm run lint && npm run format:check && npm run test",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "keywords": [
    "admin",
    "dashboard",
    "vue3",
    "express",
    "dark-theme"
  ],
  "license": "MIT",
  "devDependencies": {
    "prettier": "^3.3.3"
  }
}
